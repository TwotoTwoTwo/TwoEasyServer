<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wsjiu.dao.GoodsDAO">
    <select id="queryByGoodsId" resultType="cn.wsjiu.entity.Goods">
        select * from goods where goodsId = #{goodsId}
    </select>
    <select id="queryByLabel" resultType="cn.wsjiu.entity.Goods">
        select * from goods
            where label = #{label} limited #{page}, #{pageSize}
    </select>
    <select id="queryByUserId" resultType="cn.wsjiu.entity.Goods">
        select * from goods
            where userId = #{userId} limited #{page}, #{pageSize}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="goodsId" keyColumn="goodsId">
        insert goods(userId, title, detail, imageUrl, sellPrice, buyPrice, label, time)
            value(#{userId}, #{title}, #{detail}, #{imageUrl}, #{sellPrice}, #{buyPrice}, #{label}, #{time})
    </insert>
    <update id="update">
        update goods
        <set>
            <if test="userId != null">
                userId = #{userId},
            </if>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="detail != null">
                detail = #{detail},
            </if>
            <if test="imageUrl != null">
                imageUrl = #{imageUrl},
            </if>
            <if test="sellPrice != null">
                sellPrice = #{sellPrice},
            </if>
            <if test="buyPrice != null">
                buyPrice = #{buyPrice},
            </if>
            <if test="label != null">
                label = #{label},
            </if>
            <if test="time != null">
                time = #{time}
            </if>
        </set>
        where goodsId = #{goodsId}
    </update>
</mapper>